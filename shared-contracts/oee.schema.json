{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://mentoredge.io/schemas/oee-record.v1.json",
  "title": "OEE Record",
  "description": "Snapshot periodico de metricas OEE enviado desde edge hacia cloud. Formato columnar compacto head/data.",
  "type": "object",
  "required": ["code", "time", "device_id", "interval_s", "v", "head", "data"],
  "additionalProperties": false,
  "properties": {
    "code": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Identificador unico de linea configurado en la UI",
      "examples": ["TEXTIL_LIMA_CORTE_L1", "Linea 1"]
    },
    "time": {
      "type": "integer",
      "minimum": 0,
      "description": "Unix timestamp en milisegundos del cierre de la ventana de agregacion"
    },
    "device_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "description": "Identificador del dispositivo edge que genera el registro",
      "examples": ["jetson-orin-01"]
    },
    "interval_s": {
      "type": "integer",
      "minimum": 1,
      "description": "Duracion de la ventana de agregacion en segundos. Permite al receptor reconstruir la linea de tiempo."
    },
    "v": {
      "type": "integer",
      "minimum": 1,
      "description": "Version del esquema. Permite evolucion sin romper compatibilidad."
    },
    "head": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 17,
      "maxItems": 17,
      "description": "Nombres de las columnas de metricas",
      "examples": [[
        "T_DISPONIBLE",
        "T_MICROPARADA",
        "T_PARADA_NO_ASIGNADA",
        "T_PARADA_MAYOR",
        "CONTEO_1",
        "MARCA",
        "TAMANIO",
        "MATERIAL",
        "DESTINO",
        "T_REFRIGERIO",
        "T_CAPACITACION_OBLIGATORIA",
        "T_MANTENIMIENTO_PLANIFICADO",
        "T_PARADA_PROGRAMADA",
        "T_PARADA_NO_PROGRAMADA",
        "TIPO_PARADA_PROGRAMADA",
        "TIPO_PARADA_NO_PROGRAMADA",
        "MERMA"
      ]]
    },
    "data": {
      "type": "array",
      "items": {
        "oneOf": [
          { "type": "integer", "minimum": 0 },
          { "type": "string" }
        ]
      },
      "minItems": 17,
      "maxItems": 17,
      "description": "Valores correspondientes a head. Tiempos y conteos son enteros; atributos textiles son cadenas."
    }
  }
}
